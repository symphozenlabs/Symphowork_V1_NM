"use client";
import { useState } from "react";
import { useParams } from "next/navigation";
export default function PublicApplyPage() { const params = useParams<{ slug: string }>(); const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setBusy(true); const form = new FormData(event.currentTarget); form.set("consentStatus", form.get("consentStatus") === "on" ? "granted" : "not_granted"); const response = await fetch(`/api/public/jobs/${params.slug}/apply`, { method: "POST", body: form }); setMessage(response.ok ? "Application submitted successfully." : "We could not submit this application. Please verify the details and try again."); setBusy(false); } return <main className="min-h-screen bg-background px-5 py-12"><form onSubmit={submit} className="mx-auto max-w-2xl space-y-5 rounded-2xl border bg-surface p-6 shadow-sm"><div><p className="text-sm font-semibold uppercase tracking-[0.18em] text-muted">Job application</p><h1 className="mt-2 text-3xl font-bold">Tell us about yourself</h1><p className="mt-2 text-sm text-muted">No account is required. Your application is stored securely with the hiring organization.</p></div><div className="grid gap-4 sm:grid-cols-2"><input name="firstName" required placeholder="First name" className="h-11 rounded-lg border bg-background px-3" /><input name="lastName" required placeholder="Last name" className="h-11 rounded-lg border bg-background px-3" /><input name="email" type="email" required placeholder="Email" className="h-11 rounded-lg border bg-background px-3" /><input name="phone" placeholder="Phone" className="h-11 rounded-lg border bg-background px-3" /><input name="location" placeholder="Location" className="h-11 rounded-lg border bg-background px-3" /><input name="currentCompany" placeholder="Current company" className="h-11 rounded-lg border bg-background px-3" /><input name="currentDesignation" placeholder="Current designation" className="h-11 rounded-lg border bg-background px-3" /><input name="totalExperience" type="number" min="0" step="0.1" placeholder="Experience in years" className="h-11 rounded-lg border bg-background px-3" /><input name="expectedSalary" type="number" min="0" placeholder="Expected salary" className="h-11 rounded-lg border bg-background px-3" /><input name="noticePeriodDays" type="number" min="0" placeholder="Notice period (days)" className="h-11 rounded-lg border bg-background px-3" /></div><textarea name="coverNote" placeholder="Cover note" className="min-h-28 w-full rounded-lg border bg-background p-3" /><label className="grid gap-2 text-sm">Resume (PDF, DOC, or DOCX)<input name="resume" type="file" accept=".pdf,.doc,.docx" required className="rounded-lg border bg-background p-2" /></label><label className="flex items-start gap-2 text-sm"><input name="consentStatus" type="checkbox" required className="mt-1" />I consent to this organization processing my application for recruitment.</label>{message && <p role="status" className="rounded-lg bg-background p-3 text-sm">{message}</p>}<button disabled={busy} className="rounded-xl bg-[#10233f] px-5 py-3 font-semibold text-white">{busy ? "Submitting…" : "Submit application"}</button></form></main>; }
