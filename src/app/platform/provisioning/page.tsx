import { redirect } from "next/navigation";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { authorizePlatform, PLATFORM_PERMISSIONS } from "@/modules/platform/authorization";
import { listProvisioningJobs } from "@/modules/platform/operations";
export default async function ProvisioningPage() { try { await authorizePlatform(PLATFORM_PERMISSIONS.provisioningView); } catch { redirect("/platform/login"); } const rows = await listProvisioningJobs(); return <div className="space-y-6"><div><Badge>Provisioning</Badge><h1 className="mt-3 text-3xl font-bold">Provisioning operations</h1><p className="mt-2 text-sm text-muted">Actual job state from the organization provisioning boundary.</p></div><Card><CardHeader><CardTitle>Provisioning jobs</CardTitle></CardHeader><CardContent><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b text-xs uppercase tracking-wide text-muted"><tr><th className="px-3 py-3">Organization</th><th className="px-3 py-3">Type</th><th className="px-3 py-3">Status</th><th className="px-3 py-3">Step</th><th className="px-3 py-3">Attempts</th><th className="px-3 py-3">Created</th></tr></thead><tbody>{rows.map(({ job, organization }) => <tr key={job.id} className="border-b last:border-0"><td className="px-3 py-4 font-semibold">{organization.name}</td><td className="px-3 py-4 text-muted">{job.jobType}</td><td className="px-3 py-4"><Badge>{job.status}</Badge></td><td className="px-3 py-4 text-muted">{job.currentStep}</td><td className="px-3 py-4">{job.attempts}</td><td className="px-3 py-4 text-muted">{job.createdAt.toLocaleString()}</td></tr>)}</tbody></table>{!rows.length && <p className="p-8 text-center text-sm text-muted">No provisioning jobs found.</p>}</div></CardContent></Card></div>; }
