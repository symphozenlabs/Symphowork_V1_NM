import Link from "next/link";
import { redirect } from "next/navigation";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { authorizePlatform, PLATFORM_PERMISSIONS } from "@/modules/platform/authorization";
import { listPlatformOrganizations } from "@/modules/platform/operations";
export default async function OrganizationsPage() { try { await authorizePlatform(PLATFORM_PERMISSIONS.organizationView); } catch { redirect("/platform/login"); } const result = await listPlatformOrganizations({ pageSize: 50 }); return <div className="space-y-6"><div className="flex items-end justify-between"><div><Badge>Organizations</Badge><h1 className="mt-3 text-3xl font-bold">Tenant operations</h1><p className="mt-2 text-sm text-muted">Inspect organization lifecycle without entering tenant membership context.</p></div><Button asChild><Link href="/platform/organizations/new">Create organization</Link></Button></div><Card><CardHeader><CardTitle>All organizations <span className="ml-2 text-sm font-normal text-muted">{result.total}</span></CardTitle></CardHeader><CardContent><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b text-xs uppercase tracking-wide text-muted"><tr><th className="px-3 py-3">Organization</th><th className="px-3 py-3">Identifier</th><th className="px-3 py-3">Status</th><th className="px-3 py-3">Created</th></tr></thead><tbody>{result.rows.map((row) => <tr key={row.id} className="border-b last:border-0"><td className="px-3 py-4 font-semibold"><Link className="hover:text-primary" href={`/platform/organizations/${row.id}`}>{row.name}</Link></td><td className="px-3 py-4 text-muted">{row.slug}</td><td className="px-3 py-4"><Badge>{row.status}</Badge></td><td className="px-3 py-4 text-muted">{row.createdAt.toLocaleDateString()}</td></tr>)}</tbody></table>{!result.rows.length && <p className="p-8 text-center text-sm text-muted">No organizations match the current scope.</p>}</div></CardContent></Card></div>; }
