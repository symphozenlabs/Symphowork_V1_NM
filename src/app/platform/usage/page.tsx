import { redirect } from "next/navigation";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { authorizePlatform, PLATFORM_PERMISSIONS } from "@/modules/platform/authorization";
import { listOrganizationUsage } from "@/modules/platform/commercial";
export default async function UsagePage() { try { await authorizePlatform(PLATFORM_PERMISSIONS.usageView); } catch { redirect("/platform/login"); } const rows = await listOrganizationUsage(); return <div className="space-y-6"><div><Badge>Licensing</Badge><h1 className="mt-3 text-3xl font-bold">Usage & licensing</h1><p className="mt-2 text-sm text-muted">Active employees are counted from the existing employee lifecycle model.</p></div><Card><CardHeader><CardTitle>Employee capacity</CardTitle></CardHeader><CardContent><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b text-xs uppercase tracking-wide text-muted"><tr><th className="px-3 py-3">Organization</th><th className="px-3 py-3">Plan</th><th className="px-3 py-3">Active employees</th><th className="px-3 py-3">Allowed</th><th className="px-3 py-3">Remaining</th><th className="px-3 py-3">Status</th></tr></thead><tbody>{rows.map(({ organization, plan, activeEmployees, employeeLimit, remaining, overLimit }) => <tr key={organization.id} className="border-b last:border-0"><td className="px-3 py-4 font-semibold">{organization.name}</td><td className="px-3 py-4">{plan?.name ?? "No plan"}</td><td className="px-3 py-4">{activeEmployees}</td><td className="px-3 py-4">{employeeLimit ?? "Unlimited"}</td><td className="px-3 py-4">{remaining ?? "Unlimited"}</td><td className="px-3 py-4"><Badge>{overLimit ? "Over limit" : "Within limit"}</Badge></td></tr>)}</tbody></table>{!rows.length && <p className="p-8 text-center text-sm text-muted">No organization usage found.</p>}</div></CardContent></Card></div>; }
