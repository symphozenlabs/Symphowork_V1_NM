import { NextResponse } from "next/server";
import { sql } from "@/db/client";
export async function GET() { const databaseConfigured = Boolean(process.env.DATABASE_URL); let database = "not_configured"; if (databaseConfigured) { try { await sql`select 1`; database = "ok"; } catch { database = "unavailable"; } } const healthy = database === "ok" || database === "not_configured"; return NextResponse.json({ status: healthy ? "ok" : "degraded", service: "symphowork", timestamp: new Date().toISOString(), checks: { database, emailProvider: process.env.EMAIL_PROVIDER ? "configured" : "not_configured", storageProvider: process.env.OBJECT_STORAGE_PROVIDER ? "configured" : "not_configured", aiProvider: process.env.AI_PROVIDER ? "configured" : "not_configured", billingProvider: process.env.BILLING_PROVIDER ? "configured" : "not_configured", realtimeProvider: process.env.REALTIME_PROVIDER ? "configured" : "not_configured" } }, { status: healthy ? 200 : 503 }); }
