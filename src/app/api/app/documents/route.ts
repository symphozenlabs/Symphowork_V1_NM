import { NextResponse } from "next/server";
import { errorResponse, AppError } from "@/lib/errors";
import { resolveTenantContext } from "@/modules/tenancy/context";
import { createEmployeeDocument, listEmployeeDocuments } from "@/modules/documents/service";
export async function GET() { try { const tenant = await resolveTenantContext(); if (!tenant.organization) throw new AppError("FORBIDDEN", "Organization context is required.", 403); return NextResponse.json({ success: true, documents: await listEmployeeDocuments({ organizationId: tenant.organization.id, userId: tenant.user.id }) }); } catch (error) { return errorResponse(error); } }
export async function POST(request: Request) { try { const tenant = await resolveTenantContext(); if (!tenant.organization) throw new AppError("FORBIDDEN", "Organization context is required.", 403); const form = await request.formData(); const file = form.get("file"); const documentTypeId = String(form.get("documentTypeId") ?? ""); if (!(file instanceof File) || !documentTypeId) throw new AppError("VALIDATION_ERROR", "A document type and file are required.", 400); return NextResponse.json({ success: true, document: await createEmployeeDocument({ organizationId: tenant.organization.id, userId: tenant.user.id, documentTypeId, fileName: file.name, mimeType: file.type, bytes: new Uint8Array(await file.arrayBuffer()), expiresAt: form.get("expiresAt")?.toString() || undefined, replacedDocumentId: form.get("replacedDocumentId")?.toString() || undefined }) }, { status: 201 }); } catch (error) { return errorResponse(error); } }
