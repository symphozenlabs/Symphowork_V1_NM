import { NextResponse } from "next/server";
import { errorResponse, AppError } from "@/lib/errors";
import { resolveTenantContext } from "@/modules/tenancy/context";
import { actOnApprovalTask, type WorkflowAction } from "@/modules/workflows/service";
import { db } from "@/db/client";
import { attendanceRecords, attendanceRegularizationRequests, leaveApplications, leaveBalances, leaveTransactions } from "@/db/schema";
import { and, eq } from "drizzle-orm";

export async function POST(request: Request, context: { params: Promise<{ taskId: string }> }) { try { const tenant = await resolveTenantContext(); if (!tenant.organization) throw new AppError("FORBIDDEN", "Organization context is required.", 403); const body = await request.json() as { action?: WorkflowAction; comment?: string }; if (!body.action || !["approve", "reject", "cancel"].includes(body.action)) throw new AppError("VALIDATION_ERROR", "A valid approval action is required.", 400); const { taskId } = await context.params; const result = await actOnApprovalTask({ organizationId: tenant.organization.id, taskId, actorUserId: tenant.user.id, action: body.action, comment: body.comment }); await db.transaction(async (tx) => { if (result.entityType === "leave_application") { const [application] = await tx.select().from(leaveApplications).where(and(eq(leaveApplications.id, result.entityId), eq(leaveApplications.organizationId, tenant.organization!.id))); if (application && result.status !== "in_progress") { const [balance] = await tx.select().from(leaveBalances).where(and(eq(leaveBalances.organizationId, tenant.organization!.id), eq(leaveBalances.employeeId, application.employeeId), eq(leaveBalances.leaveTypeId, application.leaveTypeId), eq(leaveBalances.leaveYear, Number(application.startDate.slice(0, 4))))); if (balance) { const approved = result.status === "approved"; await tx.update(leaveApplications).set({ status: approved ? "approved" : result.status === "cancelled" ? "cancelled" : "rejected", approvedAt: approved ? new Date() : undefined, rejectedAt: !approved ? new Date() : undefined, updatedAt: new Date() }).where(eq(leaveApplications.id, application.id)); await tx.update(leaveBalances).set({ pending: Math.max(0, balance.pending - application.requestedDays), consumed: approved ? balance.consumed + application.requestedDays : balance.consumed, updatedAt: new Date() }).where(eq(leaveBalances.id, balance.id)); await tx.insert(leaveTransactions).values({ organizationId: tenant.organization!.id, employeeId: application.employeeId, leaveTypeId: application.leaveTypeId, leaveBalanceId: balance.id, transactionType: approved ? "consumption" : "reservation_release", amount: application.requestedDays, referenceId: application.id, actorUserId: tenant.user.id }); } } } else if (result.entityType === "attendance_regularization" && result.status !== "in_progress") { const [regularization] = await tx.select().from(attendanceRegularizationRequests).where(and(eq(attendanceRegularizationRequests.id, result.entityId), eq(attendanceRegularizationRequests.organizationId, tenant.organization!.id))); if (regularization) { const approved = result.status === "approved"; await tx.update(attendanceRegularizationRequests).set({ status: approved ? "approved" : "rejected", resolvedAt: new Date(), resolvedByUserId: tenant.user.id, updatedAt: new Date() }).where(eq(attendanceRegularizationRequests.id, regularization.id)); if (approved) await tx.update(attendanceRecords).set({ actualFirstIn: regularization.requestedIn, actualLastOut: regularization.requestedOut, status: "regularized", updatedAt: new Date() }).where(eq(attendanceRecords.id, regularization.attendanceId)); } } }); return NextResponse.json({ success: true, result }); } catch (error) { return errorResponse(error); } }
