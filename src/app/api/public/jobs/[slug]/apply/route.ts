import { NextResponse } from "next/server";
import { z } from "zod";
import { errorResponse } from "@/lib/errors";
import { createApplication } from "@/modules/ats/service";
const publicApplication = z.object({ firstName: z.string().trim().min(1).max(80), lastName: z.string().trim().min(1).max(80), email: z.string().email(), phone: z.string().max(40).optional(), location: z.string().max(160).optional(), currentCompany: z.string().max(180).optional(), currentDesignation: z.string().max(160).optional(), totalExperience: z.number().min(0).max(80).optional(), expectedSalary: z.number().min(0).optional(), noticePeriodDays: z.number().int().min(0).max(365).optional(), coverNote: z.string().max(5000).optional(), consentStatus: z.literal("granted") });
export async function POST(request: Request, context: { params: Promise<{ slug: string }> }) { try { const { slug } = await context.params; const body = publicApplication.parse(await request.json()); const result = await import("@/modules/ats/service").then(async ({ publicJobBySlug }) => { const { job } = await publicJobBySlug(slug); return createApplication({ organizationId: job.organizationId, jobId: job.id, candidateData: body, coverNote: body.coverNote }); }); return NextResponse.json({ success: true, application: { id: result.id, status: result.status } }, { status: 201 }); } catch (error) { return errorResponse(error); } }
