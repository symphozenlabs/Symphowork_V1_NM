import { redirect } from "next/navigation";
import { getSessionUser } from "@/modules/identity/auth";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
export default async function WorkspacePage() { const user = await getSessionUser(); if (!user) redirect("/login"); return <div className="space-y-6"><div><Badge>Authenticated workspace</Badge><h1 className="mt-3 text-3xl font-bold">Welcome, {user.fullName}.</h1><p className="mt-2 text-sm text-muted">Your identity is verified. Organization access will appear here after membership is provisioned.</p></div><Card><CardHeader><CardTitle>Workspace readiness</CardTitle></CardHeader><CardContent className="grid gap-3 sm:grid-cols-2"><div className="rounded-xl border p-4"><p className="text-xs uppercase tracking-wide text-muted">Account</p><p className="mt-2 font-semibold">{user.email}</p><p className="mt-1 text-sm text-success">Verified identity</p></div><div className="rounded-xl border p-4"><p className="text-xs uppercase tracking-wide text-muted">Organization modules</p><p className="mt-2 font-semibold">Coming in later milestones</p><p className="mt-1 text-sm text-muted">No HRMS or ATS functionality is enabled yet.</p></div></CardContent></Card></div>; }
