import { z } from "zod";
export const employeeInputSchema = z.object({ firstName: z.string().trim().min(1).max(80), middleName: z.string().trim().max(80).optional(), lastName: z.string().trim().min(1).max(80), personalEmail: z.string().email().optional().or(z.literal("")), workEmail: z.string().email().optional().or(z.literal("")), phone: z.string().max(40).optional(), joiningDate: z.string().optional(), employmentTypeId: z.string().uuid().optional(), departmentId: z.string().uuid().optional(), designationId: z.string().uuid().optional(), reportingManagerId: z.string().uuid().nullable().optional(), teamId: z.string().uuid().optional(), locationId: z.string().uuid().optional(), shiftId: z.string().uuid().optional(), status: z.enum(["invited", "onboarding", "active", "probation", "confirmed", "notice_period", "inactive", "resigned", "terminated", "exited"]).default("invited") });
export type EmployeeInput = z.infer<typeof employeeInputSchema>;
