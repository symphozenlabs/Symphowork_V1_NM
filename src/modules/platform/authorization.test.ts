import { describe, expect, it } from "vitest";
import { hasPlatformPermission, PLATFORM_PERMISSIONS } from "./authorization";
describe("platform authorization boundary", () => { it("does not treat organization roles as platform roles", () => { expect(hasPlatformPermission("ORGANIZATION_ADMIN", PLATFORM_PERMISSIONS.organizationView)).toBe(false); expect(hasPlatformPermission("EMPLOYEE", PLATFORM_PERMISSIONS.organizationView)).toBe(false); }); it("limits platform role capabilities", () => { expect(hasPlatformPermission("PRODUCT_OWNER", PLATFORM_PERMISSIONS.billingManage)).toBe(true); expect(hasPlatformPermission("PLATFORM_SUPPORT", PLATFORM_PERMISSIONS.billingManage)).toBe(false); expect(hasPlatformPermission("PLATFORM_BILLING", PLATFORM_PERMISSIONS.billingManage)).toBe(true); }); });
