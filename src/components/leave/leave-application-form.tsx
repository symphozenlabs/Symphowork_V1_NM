"use client";
import { useEffect, useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

export function LeaveApplicationForm() { const [types, setTypes] = useState<Array<{ id: string; name: string }>>([]); const [form, setForm] = useState({ leaveTypeId: "", startDate: "", endDate: "", reason: "" }); const [message, setMessage] = useState(""); useEffect(() => { void fetch("/api/app/leave/types").then((response) => response.json()).then((body) => { if (body.success) setTypes(body.leaveTypes); }); }, []); const submit = async (event: React.FormEvent) => { event.preventDefault(); setMessage("Submitting…"); const response = await fetch("/api/app/leave", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(form) }); const body = await response.json(); setMessage(body.success ? "Leave request submitted for approval." : body.error?.message ?? "Unable to submit request."); }; return <form onSubmit={submit} className="grid gap-4 md:grid-cols-2"><label className="grid gap-1 text-sm">Leave type<select required className="h-10 rounded-lg border bg-surface px-3" value={form.leaveTypeId} onChange={(event) => setForm({ ...form, leaveTypeId: event.target.value })}><option value="">Select a type</option>{types.map((type) => <option key={type.id} value={type.id}>{type.name}</option>)}</select></label><label className="grid gap-1 text-sm">Start date<Input required type="date" value={form.startDate} onChange={(event) => setForm({ ...form, startDate: event.target.value })} /></label><label className="grid gap-1 text-sm">End date<Input required type="date" value={form.endDate} onChange={(event) => setForm({ ...form, endDate: event.target.value })} /></label><label className="grid gap-1 text-sm md:col-span-2">Reason<textarea required className="min-h-24 rounded-lg border bg-surface p-3" value={form.reason} onChange={(event) => setForm({ ...form, reason: event.target.value })} /></label><div className="md:col-span-2"><Button type="submit">Submit request</Button><p aria-live="polite" className="mt-2 text-sm text-muted">{message}</p></div></form>; }
