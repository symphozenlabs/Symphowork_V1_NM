"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
export function PlanEditor({ plan }: { plan?: { id: string; code: string; name: string; description: string | null; monthlyPriceCents: number | null; annualPriceCents: number | null; currency: string; trialDays: number; maxUsers: number | null; maxStorageBytes: number | null; billingInterval: string; active: boolean } }) { const [message, setMessage] = useState(""); const [saving, setSaving] = useState(false); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setSaving(true); const form = new FormData(event.currentTarget); const number = (key: string) => { const value = form.get(key)?.toString() ?? ""; return value === "" ? null : Number(value); }; const payload = { code: form.get("code"), name: form.get("name"), description: form.get("description") || undefined, monthlyPriceCents: number("monthlyPriceCents"), annualPriceCents: number("annualPriceCents"), currency: form.get("currency"), trialDays: Number(form.get("trialDays") ?? 0), maxUsers: number("maxUsers"), maxStorageBytes: number("maxStorageBytes"), billingInterval: form.get("billingInterval"), active: form.get("active") === "on" }; const response = await fetch(plan ? `/api/platform/plans/${plan.id}` : "/api/platform/plans", { method: plan ? "PATCH" : "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(payload) }); const body = await response.json(); setMessage(response.ok ? "Saved." : body.error?.message ?? "Unable to save."); setSaving(false); if (response.ok) window.location.reload(); } return <form onSubmit={submit} className="grid gap-3 rounded-xl border p-4 sm:grid-cols-2"><label className="text-sm">Code<input name="code" defaultValue={plan?.code ?? "FREE"} readOnly={Boolean(plan)} className="mt-1 w-full rounded-lg border p-2" /></label><label className="text-sm">Name<input name="name" defaultValue={plan?.name} required className="mt-1 w-full rounded-lg border p-2" /></label><label className="text-sm sm:col-span-2">Description<input name="description" defaultValue={plan?.description ?? ""} className="mt-1 w-full rounded-lg border p-2" /></label><label className="text-sm">Monthly price (minor units)<input name="monthlyPriceCents" type="number" min="0" defaultValue={plan?.monthlyPriceCents ?? ""} className="mt-1 w-full rounded-lg border p-2" /></label><label className="text-sm">Annual price (minor units)<input name="annualPriceCents" type="number" min="0" defaultValue={plan?.annualPriceCents ?? ""} className="mt-1 w-full rounded-lg border p-2" /></label><label className="text-sm">Currency<input name="currency" defaultValue={plan?.currency ?? "INR"} maxLength={3} className="mt-1 w-full rounded-lg border p-2" /></label><label className="text-sm">Trial days<input name="trialDays" type="number" min="0" defaultValue={plan?.trialDays ?? 0} className="mt-1 w-full rounded-lg border p-2" /></label><label className="text-sm">Active employees limit<input name="maxUsers" type="number" min="0" defaultValue={plan?.maxUsers ?? ""} className="mt-1 w-full rounded-lg border p-2" /></label><label className="text-sm">Billing interval<select name="billingInterval" defaultValue={plan?.billingInterval ?? "monthly"} className="mt-1 w-full rounded-lg border p-2"><option value="monthly">Monthly</option><option value="annual">Annual</option></select></label><label className="flex items-center gap-2 text-sm sm:col-span-2"><input name="active" type="checkbox" defaultChecked={plan?.active ?? true} /> Active</label><div className="sm:col-span-2"><Button disabled={saving}>{saving ? "Saving…" : plan ? "Save plan" : "Create plan"}</Button>{message && <span className="ml-3 text-sm text-muted">{message}</span>}</div></form>; }
