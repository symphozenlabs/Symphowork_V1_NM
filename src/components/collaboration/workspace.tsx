"use client";
import { useEffect, useState } from "react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
type Project = { id: string; projectNumber: string; name: string; status: string; priority: string; description?: string | null };
type Task = { id: string; taskNumber: string; title: string; status: string; priority: string; dueDate?: string | null };
export function CollaborationWorkspace() { const [projects, setProjects] = useState<Project[]>([]); const [tasks, setTasks] = useState<Task[]>([]); useEffect(() => { void Promise.all([fetch("/api/app/projects").then((response) => response.json()).then((body) => setProjects(body.projects ?? [])), fetch("/api/app/tasks").then((response) => response.json()).then((body) => setTasks(body.tasks ?? []))]); }, []); return <div className="grid gap-6 lg:grid-cols-2"><Card><CardHeader><CardTitle>Projects</CardTitle></CardHeader><CardContent className="space-y-3">{projects.length ? projects.map((project) => <div key={project.id} className="rounded-lg border p-3"><div className="flex items-center justify-between"><span className="font-medium">{project.name}</span><Badge>{project.status}</Badge></div><p className="text-xs text-muted">{project.projectNumber} · {project.priority}</p></div>) : <p className="text-sm text-muted">No projects are visible yet.</p>}</CardContent></Card><Card><CardHeader><CardTitle>My work</CardTitle></CardHeader><CardContent className="space-y-3">{tasks.length ? tasks.map((task) => <div key={task.id} className="rounded-lg border p-3"><div className="flex items-center justify-between"><span className="font-medium">{task.title}</span><Badge>{task.status}</Badge></div><p className="text-xs text-muted">{task.taskNumber} · {task.priority} · due {task.dueDate ?? "not set"}</p></div>) : <p className="text-sm text-muted">No tasks assigned to you yet.</p>}</CardContent></Card></div>; }
